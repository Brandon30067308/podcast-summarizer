{
  "name": "podcast",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/axios": "^0.14.4",
    "prettier": "^3.2.5",
    "turbo": "^2.4.4",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
